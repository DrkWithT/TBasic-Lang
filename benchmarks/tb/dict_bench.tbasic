`dict lookup speed benchmark`

LET data : {
    "i" : 5000;
    "a" : 0;
};

WHILE data["i"] > 0:
    data["a"] := data["a"] + data["i"];
    data["i"] := data["i"] - 1;
END

ASSERT data["a"] == 12502500, "Expected 12502500 for 1 to 5K sum.";

RET TRUE;
