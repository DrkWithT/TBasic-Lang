`test for loops`

FUN sumN(n):
    LET total : 0, m : 0;

    FOR m : n, m > 0, m := (m - 1); :
        total := (total + m);
    END

    RET total;
END

ASSERT sumN(10) == 55, "Expected sum of 1-10 as 55";

RET TRUE;
